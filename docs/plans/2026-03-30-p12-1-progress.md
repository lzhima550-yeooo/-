# P12-1 Progress (2026-03-31)

## Completed
- Added community knowledge backflow pipeline: `extract -> candidate list -> manual approve`.
- Added API endpoints:
  - `POST /api/community/backflow/extract`
  - `GET /api/community/backflow/candidates`
  - `POST /api/community/backflow/candidates/:id/approve`
- Added request validators for extract/approve payloads.
- Added Supabase persistence methods for:
  - candidate generation and deduplicated insert
  - candidate list filtering
  - manual approval write-back to `source_index_items` or `treatment_templates`
- Added schema table `knowledge_backflow_candidates` with indexes, RLS read policy, and `updated_at` trigger.
- Added backend contract test:
  - `server/__tests__/community-backflow.contract.test.ts`
- Fixed Supabase `or` filter syntax in service queries (wrapped with parentheses) to avoid keyword-query 400 errors during real extraction.

## Verification Evidence
- `npm run test:run -- server/__tests__/community-backflow.contract.test.ts --pool=threads --maxWorkers=1` PASS (3 tests).
- `npm run test:run -- server/__tests__/chat-stream-memory.contract.test.ts server/__tests__/encyclopedia-evidence.contract.test.ts --pool=threads --maxWorkers=1` PASS (5 tests).
- `npm run test:api` PASS (23 tests).
- `npm run build` PASS.

## Next (P12-2)
- Produce release demo checklist: `docs/release/final-demo-checklist.md`.
- Update operations runbook for recovery + offline/real API mode switch notes.
- Add one-click rehearsal script for:
  - identify task
  - action cards
  - encyclopedia detail evidence
  - chat stream memory
  - draft publish
